import HelpPopover from "@/app/products/pcb/_components/common/helpPopover";

export default function OuterCuWeightTip() {
  return (
    <HelpPopover>
      <p>
        Copper weight on the outer layers(top and bottom). The copper weight for
        inner layers is 0.5 oz by default.
      </p>
    </HelpPopover>
  );
}
