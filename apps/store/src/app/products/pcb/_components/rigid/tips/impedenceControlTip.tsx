import HelpPopover from "@/app/products/pcb/_components/common/helpPopover";

export default function ImpedenceControlTip() {
  return (
    <HelpPopover>
      <p>
        We offer high precision Controlled Impedance PCB at extra charge, with a
        minimum trace width of 3.5mil, minimum via size of 0.2mm, and minimum
        BGA of 0.3mm.
      </p>
    </HelpPopover>
  );
}
