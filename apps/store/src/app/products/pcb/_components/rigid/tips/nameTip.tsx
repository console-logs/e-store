import HelpPopover from "@/app/products/pcb/_components/common/helpPopover";

export default function NameTip() {
  return (
    <HelpPopover>
      <p>
        This is the name of your PCB. It is used for the reference in the order
        confirmation email.
      </p>
    </HelpPopover>
  );
}
