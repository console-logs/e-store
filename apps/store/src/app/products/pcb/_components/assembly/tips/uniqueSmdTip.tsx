import HelpPopover from "@/app/products/pcb/_components/common/helpPopover";

export default function UniqueSmdTip() {
	return (
		<HelpPopover>
			<p>How many number of SMD components are present on the board.</p>
		</HelpPopover>
	);
}
