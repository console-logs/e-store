import HelpPopover from "@/app/products/pcb/_components/common/helpPopover";

export default function DepanelTip() {
	return (
		<HelpPopover>
			<p>Should we de-panel your board?</p>
		</HelpPopover>
	);
}
