import HelpPopover from "@/app/products/pcb/_components/common/helpPopover";

export default function TempAndHumidityTip() {
	return (
		<HelpPopover>
			<p>The temperature and humidity sensitivity tolerance of your components</p>
		</HelpPopover>
	);
}
