import HelpPopover from "@/app/products/pcb/_components/common/helpPopover";

export default function ConformalCoatTip() {
	return (
		<HelpPopover>
			<p>Should we add a conformal coating to your board?</p>
		</HelpPopover>
	);
}
