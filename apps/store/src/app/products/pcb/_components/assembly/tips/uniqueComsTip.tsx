import HelpPopover from "@/app/products/pcb/_components/common/helpPopover";

export default function UniqueComponentsTip() {
	return (
		<HelpPopover>
			<p>How many number of unique components are present on the board.</p>
		</HelpPopover>
	);
}
