import HelpPopover from "@/app/products/pcb/_components/common/helpPopover";

export default function AssemblyQtyTip() {
	return (
		<HelpPopover>
			<p>The quantity you&apos;d want to get assembled</p>
		</HelpPopover>
	);
}
