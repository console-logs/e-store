import HelpPopover from "@/app/products/pcb/_components/common/helpPopover";

export default function ComponentSourcingTip() {
	return (
		<HelpPopover>
			<p>Should we source the components on your behalf?</p>
		</HelpPopover>
	);
}
